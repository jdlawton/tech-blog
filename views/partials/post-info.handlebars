<!--
<article class="post">
    <div class="title"><a href="/post/{{id}}">{{post.title}}</a></div>
    <div>{{post.content}}</div>
    <div class="meta">
        posted by {{post.user.username}} on {{post.created_at}}
    </div>
</article> -->


<article class="post">
    <div class="title"><a href="/post/{{id}}">{{title}}</a></div>
    <div>{{content}}</div>
    <div class="meta">
        <p>posted by {{user.username}} on {{format_date created_at}}</p>
    </div>
    <div>{{post.content}}</div>
</article>